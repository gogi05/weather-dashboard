const DetailsChart = ({ chartData }) => {
  console.log("ChartData to be used: ", chartData);
  return "Here chart is to be renderd (Check console for the data fetched)";
};

export default DetailsChart;
